<!doctype html>
<html lang="pl">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Moje portfolio</title>
  <!-- Google Font -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap" rel="stylesheet">
  <style>
    :root{--bg1:#0f172a;--bg2:#0b1220;--accent1:#7c3aed;--accent2:#06b6d4}
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      font-family:Inter,system-ui,-apple-system,"Segoe UI",Roboto,"Helvetica Neue",Arial;
      color:#e6eef8;
      background:linear-gradient(135deg,var(--bg1),var(--bg2));
      display:flex;
      align-items:center;
      justify-content:center;
      padding:2rem;
    }
    .container{
      max-width:900px;
      width:100%;
      background:linear-gradient(180deg, rgba(255,255,255,0.03), rgba(255,255,255,0.01));
      padding:3rem;
      border-radius:16px;
      box-shadow:0 8px 30px rgba(2,6,23,0.6);
      text-align:center;
    }
    h1{
      margin:0 0 0.5rem;
      font-size:clamp(2rem,6vw,4rem);
      letter-spacing:-0.02em;
      background:linear-gradient(90deg,var(--accent1),var(--accent2));
      -webkit-background-clip:text;
      background-clip:text;
      color:transparent;
    }
    p.lead{
      margin:0 0 1.5rem;
      color:rgba(230,238,248,0.8);
      font-size:1.05rem;
    }
    .hr{
      height:1px;
      background:linear-gradient(90deg,transparent,rgba(255,255,255,0.06),transparent);
      margin:1.5rem 0;
      border-radius:1px;
    }
    .buttons{
      display:flex;
      gap:0.75rem;
      justify-content:center;
      flex-wrap:wrap;
    }
    .btn{
      display:inline-flex;
      align-items:center;
      gap:0.5rem;
      padding:0.65rem 1rem;
      border-radius:10px;
      text-decoration:none;
      font-weight:600;
      transition:transform .12s ease, box-shadow .12s ease;
      border:1px solid rgba(255,255,255,0.06);
      background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
      color:inherit;
    }
    .btn:active{transform:translateY(1px)}
    .btn.primary{
      background:linear-gradient(90deg,var(--accent1),var(--accent2));
      color:#061025;
      box-shadow:0 6px 20px rgba(108,52,255,0.18);
      border: none;
    }
    .footer{
      margin-top:1.5rem;
      font-size:0.9rem;
      color:rgba(230,238,248,0.6);
    }
    @media (max-width:520px){
      .container{ padding:2rem}
      .buttons{ flex-direction:column}
    }
  </style>
</head>
<body>
  <div class="container" role="main">
    <h1>Moje portfolio</h1>
    <p class="lead">Witaj! To jest przykładowa strona startowa. Możesz ją edytować, dodać sekcje i własne linki.</p>
    <div class="hr" aria-hidden="true"></div>
    <div class="buttons">
      <a class="btn primary" href="#" onclick="alert('Tu możesz linkować do sekcji portfolio!'); return false;">Zobacz projekty</a>
      <a class="btn" href="mailto:kontakt@example.com">Kontakt</a>
    </div>
    <div class="footer">Prosty szablon — gotowy do wdrożenia na Vercel.</div>
  </div>
</body>
</html>
